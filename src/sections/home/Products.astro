---
// COMPONENTS //
import Image from "astro/components/Image.astro";
import ToolBadge from "@/components/ToolBadge.astro";
import ProductItem from "@/components/ProductItem.astro";

// DATA //
import { homeProductsDetails, productItems } from "@/data/products";

---

<!-- Product Section -->

<section class="flex flex-col justify-center items-center">

  <!-- Container -->

  <div
    class="flex flex-col justify-center items-center px-6 md:px-10 lg:px-14 py-40 lg:32 gap-12 md:gap-16 lg:gap-20 container"
  >

    <!-- Header -->

    <div class="flex flex-col justify-center items-center gap-6 lg:gap-7">

      <!-- Tool Badge -->

      <ToolBadge
        icon={homeProductsDetails.productIcon}
        text={homeProductsDetails.toolBadgeText}
      />

      <!-- Title Wrapper -->

      <div class="flex flex-col justify-center items-center gap-5 lg:gap-6">

        <!-- Heading -->

        <div class="flex flex-wrap justify-center items-end gap-1.5">
          <p
            class="font-normal text-4xl md:text-6xl lg:text-7xl tracking-tight text-n-950 vertical-alignment-middle horizontal-alignment-center"
          >
            {homeProductsDetails.textWrapperBuiltWith}
          </p>
          <Image
            src={`/images/products/${homeProductsDetails.titleWrapperImage}`}
            alt={homeProductsDetails.titleWrapperAlt}
            width={67}
            height={50}
            class="md:w-[101px] md:h-[76px] lg:w-[118px] lg:h-[88px]"
          />
          <p
            class="font-normal text-4xl md:text-6xl lg:text-7xl tracking-tight text-n-950 vertical-alignment-middle horizontal-alignment-center"
          >
            {homeProductsDetails.textWrapperByPixolo}
          </p>
        </div>

        <!-- Message -->

        <p
          class="font-medium text-sm md:text-lg lg:text-2xl leading-normal text-n-950 text-center"
        >
          {homeProductsDetails.titleWrapperMessage}
        </p>
      </div>
    </div>

    <!-- Products -->
     
    <div>
      {
        productItems.map((product) => (
          <ProductItem
            companyLogo={product.companyLogo}
            companyName={product.companyName}
            imageName={product.imageName}
            name={product.name}
            description={product.description}
            href={product.href}
          />
        ))
      }
    </div>
  </div>
</section>
